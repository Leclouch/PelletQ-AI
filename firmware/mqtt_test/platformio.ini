; ============================================================================
; PelletQ-AI ESP32 MQTT-over-WebSocket Connection Test — PlatformIO config
; ============================================================================

[platformio]
src_dir = .

[env:esp32dev]
platform      = pioarduino/platform-espressif32@54.03.20
board         = esp32dev
framework     = espidf
monitor_speed = 115200
